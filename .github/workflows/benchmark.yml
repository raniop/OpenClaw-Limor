name: Benchmarks
on:
  workflow_dispatch:  # Manual trigger only — benchmarks need API keys

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: npm run benchmark
